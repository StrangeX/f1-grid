apiVersion: v1
kind: Service
metadata:
  name: f1-grid
  namespace: f1-grid
spec:
  type: NodePort
  selector:
    app: f1-grid
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080
