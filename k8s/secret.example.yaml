# Copy to secret.yaml and fill the key, or skip — the app falls back to a template brief.
apiVersion: v1
kind: Secret
metadata:
  name: f1-grid-secrets
  namespace: f1-grid
type: Opaque
stringData:
  OPENAI_API_KEY: ""
